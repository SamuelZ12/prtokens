# prtokens test PR

This file exists to create a small, harmless pull request for validating the
`prtokens` PR comment workflow.

Expected validation:

- The pull request can be opened from this branch.
- `prtokens` can resolve the pull request.
- `prtokens` can post or update its single token usage comment.
